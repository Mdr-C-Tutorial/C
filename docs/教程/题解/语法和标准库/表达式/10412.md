# 10412：逗号运算符的结果

## 题目

解释以下代码执行后 `i` 和 `x` 的值：

```c
int i = 0;
int x = (i += 2, i *= 3);
```

## 解析

逗号运算符先求左操作数，完成其副作用，再求右操作数；整个逗号表达式的值和类型来自右操作数。

## 答案

1. `i += 2` 先把 `i` 从 0 改为 2。
2. `i *= 3` 随后把 `i` 改为 6，并产生值 6。
3. 逗号表达式的值是右操作数的值，所以 `x` 初始化为 6。

最终：

```text
i == 6
x == 6
```
