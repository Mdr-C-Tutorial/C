# 10405：按结合规则补全括号

## 题目

为四个表达式补充能显式体现原有分组方式的括号。

## 答案

1. `a + (b * c)`
2. `(a && b) || c`
3. `a = (b = c)`
4. `a ? b : (c + d)`

## 解析

- `*` 的优先级高于 `+`。
- `&&` 的优先级高于 `||`。
- 赋值运算符从右向左结合。
- `+` 的优先级高于条件运算符，所以第三操作数是 `c + d`。

条件运算符的第二操作数在语法上按一个完整表达式处理，但本题中的 `b` 不需要额外分组。
