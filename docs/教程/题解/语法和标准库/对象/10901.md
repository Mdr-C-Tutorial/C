# 10901

## 题目

给出“对象”“类型”“值”“标识符”四者的区别，并各举一个例子。

## 答案

对象是程序执行时能够存储值的一块区域。例如：

```c
int x = 3;
```

这里 `x` 所指的存储区域是一个对象。

类型规定对象或表达式的取值范围、表示方式和可用操作。例如 `int` 是类型。

值是对象或表达式在某一时刻表示的具体内容。例如上面代码中，`x` 的值是 `3`。

标识符是程序文本中的名字。例如 `x` 是一个标识符。

