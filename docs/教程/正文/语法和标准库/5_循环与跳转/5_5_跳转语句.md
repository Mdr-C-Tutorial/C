# 5.5 跳转语句

跳转语句会改变通常的控制流。`break` 和 `continue` 作用于循环或 `switch`，`return` 结束当前函数，`goto` 跳到同一函数中的标号。

## 本节内容

1. [5.5.1 `break` 与 `continue`](/教程/正文/语法和标准库/5_循环与跳转/5_5_跳转语句/5_5_1_break_continue)
2. [6.2 `return`](/教程/正文/语法和标准库/6_函数/6_2_return)
3. [5.5.2 `goto`](/教程/正文/语法和标准库/5_循环与跳转/5_5_跳转语句/5_5_2_goto)

先根据希望离开的语法范围选择对应语句。只有确实需要跳到标号时才使用 `goto`，并检查跳转后的清理和作用域约束。
