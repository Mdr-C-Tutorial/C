# 目录

1. 涵盖范围
2. 规范性引用文件
3. 术语、定义和符号
   1. 总则与行为相关术语
   2. 数据表示与程序构成相关术语
   3. 值、表示与数学符号
4. 一致性
5. 环境
   1. 概念模型
      1. 环境总览
      2. 翻译环境
      3. 执行环境
   2. 环境考虑因素
      1. 字符集
      2. 多字节字符
      3. 字符显示语义
      4. 信号和中断
      5. 环境限度
6. 语言
   1. 注解
   2. 概念
      1. 标识符、类型名和复合字面量的作用域
      2. 标识符的链接
      3. 标识符的命名空间
      4. 对象的存储期
      5. 类型
      6. 类型的表示
      7. 兼容类型和复合类型
      8. 对象的对齐
      9. 编码
   3. 转换
      1. 算术操作数
      2. 其他操作数
   4. 词法元素
      1. 关键字
      2. 标识符
      3. 通用字符名
      4. 常量
      5. 字符串字面量
      6. 标点符号
      7. 头名称
      8. 预处理数
      9. 注释
   5. 表达式
      1. 一般规定
      2. 基本表达式
      3. 后缀运算符
      4. 一元运算符
      5. 强制转换运算符
      6. 乘法运算符
      7. 加法运算符
      8. 按位移位运算符
      9. 关系运算符
      10. 相等运算符
      11. 按位与运算符
      12. 按位异或运算符
      13. 按位或运算符
      14. 逻辑与运算符
      15. 逻辑或运算符
      16. 条件运算符
      17. 赋值运算符
      18. 逗号运算符
   6. 常量表达式
   7. 声明
      1. 一般规定
      2. 存储类说明符
      3. 类型说明符
         1. 一般规定
         2. 结构体和联合体说明符
         3. 枚举说明符
         4. 标记
         5. 原子类型说明符
         6. typeof 说明符
      4. 类型限定符
         1. 一般规定
         2. restrict 的形式化定义
      5. 函数说明符
      6. 对齐说明符
      7. 声明符
         1. 一般规定
         2. 指针声明符
         3. 数组声明符
         4. 函数声明符
      8. 类型名
      9. 类型定义
      10. 类型推导
      11. 初始化
      12. 静态断言
      13. 属性
          1. 引言
          2. 一般规定
          3. nodiscard 属性
          4. maybe_unused 属性
          5. deprecated 属性
          6. fallthrough 属性
          7. noreturn 与 _Noreturn 属性
          8. 函数类型的标准属性
             1. 一般规定
   8. 语句
      1. 一般规定
      2. 带标签语句
      3. 复合语句
      4. 表达式语句和空语句
      5. 选择语句
      6. 迭代语句
      7. 跳转语句
   9. 外部定义
      1. 一般规定
      2. 函数定义
      3. 外部对象定义
   10. 预处理指令
       1. 一般规定
       2. 条件包含
       3. 源文件包含
       4. 二进制资源包含
       5. 宏替换
       6. 行控制
       7. 诊断指令
       8. pragma 指令
       9. 空指令
       10. 预定义宏名
       11. pragma 运算符
   11. 未来语言方向
       1. 浮点类型
       2. 标识符的链接
       3. 外部名称
       4. 字符转义序列
       5. 存储类说明符
       6. pragma 指令
       7. 预定义宏名
