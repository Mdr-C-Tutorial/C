# 6.10.4.2 `limit` 参数

## 约束

1. 标准嵌入参数 `limit` 在嵌入参数序列中可以出现零次或一次。
2. 它的预处理器实参子句必须存在，并且形式必须是：

::: details 形式化语法

```bnf
( constant-expression )
```

:::

3. 其中的常量表达式必须是整数常量表达式。
4. 该整数常量表达式的值不得小于 `0`。
5. 记号 `defined` 不得出现在这个常量表达式中。
