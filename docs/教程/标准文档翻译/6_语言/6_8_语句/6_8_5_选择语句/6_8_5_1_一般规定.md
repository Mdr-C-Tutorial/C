# 6.8.5.1 一般规定

## 语法

::: details 形式化语法

```bnf
selection-statement:
    if ( expression ) secondary-block
    if ( expression ) secondary-block else secondary-block
    switch ( expression ) secondary-block
```

:::

## 语义

1. 选择语句根据控制表达式的值，在一组次块中选择一个执行。
