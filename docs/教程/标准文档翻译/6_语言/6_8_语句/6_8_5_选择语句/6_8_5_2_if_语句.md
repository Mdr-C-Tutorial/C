# 6.8.5.2 if 语句

## 约束

1. `if` 语句的控制表达式必须具有标量类型。

## 语义

1. 在两种形式中，只要表达式与 `0` 比较结果不相等，就执行第一个子语句。
2. 在带 `else` 的形式中，如果表达式与 `0` 比较结果相等，就执行第二个子语句。
3. 如果第一个子语句是通过某个标签到达的，那么第二个子语句不会执行。
4. 一个 `else` 总是与语法上距离它最近、并且按语法允许与其配对的前一个 `if` 相关联。
